# Cappi Shopper — що треба знати, перш ніж щось міняти

Бот тайного гостя мережі Cappi (Одеса) з агентом, який веде програму
за ТЗ. Брат Cappi-Core: та сама порода — чистий Python без залежностей,
конфіг у `api.env`, стан у json-файлах, самоперевірка перед деплоєм.

## Стан проєкту

- 16.09.2026 — каркас; того ж дня прийшло **ТЗ v1.1** (`docs/`), і
  каркас переріс у **етап 1 (MVP)** за розділом 22 ТЗ: ручний реєстр
  тайників, хвилі, акцепт, генерація завдань за спрощеними правилами,
  анкета з блоками сайту, оператора, кухні, упаковки, кур'єра,
  самовивозу; приймання маркетологом з автоперевірками агента; заявки
  на виплату; звіти в чати. Канали: сайт, застосунок, самовивіз.
- 16.09 увечері — **етап 2, перша частина:** `syrve.py` (Cloud API,
  тільки читання: замовлення за телефоном і датою, номенклатура,
  стоп-листи) — звірка анкети з фактом, чек виплати і тайминг із Syrve;
  `loopa.py` — негатив за місяць у генератор, тікети по червоній зоні
  (Loopa ще без API запису — заготовка `надіслати_тікет`, тікети
  лежать у `тікети.jsonl` і йдуть маркетологу текстом); `провокації.py`
  + каталог рівня 1 (не частіше 1 з 3, за каналом, «тестове звернення»
  для скарг); канали Телефон і Glovo (вмикаються з «Правил», Glovo —
  групи поза загальним заліком); стоп-лист при генерації; `/еталони` —
  прив'язка карток до товарів Syrve за назвою.
- 16.09 пізно ввечері — **етап 2, друга частина:** санкції за зрив після
  акцепту (рейтинг ↓, кулдаун ×2, після другого — кнопка блеклисту),
  черга резерву за рейтингом, список співробітників
  (`~/.shopper/співробітники.txt`) при реєстрації, ротація адрес (хеш
  адреси із Syrve), SLA підтримки (`підтримка.py`), місячна зводка
  1-го числа + `для_core.json` (середня оцінка місяця для показника
  «тайний гість» у Core), алерт 80% бюджету, знеособлення анкет
  старших за 180 днів (щоночі 03:00), `/реєстр` — CSV виплат, закриття
  тікета → повторна перевірка того ж зрізу першим слотом наступної хвилі.
- 16.09 ніч — **HTTP API для Loopa** (`api.py`, порт 8788, Bearer
  `SHOPPER_API_TOKEN`, `X-Role` для персоналій): розділ «Тайники» в
  Loopa (макет узгоджено) малюється з цього API, рішення — POST-ами;
  спека `docs/API.md`. Імпорт файлів (`імпорт.py`): еталони, HR,
  новинки, зміни, ABC — просто файлом у чат. Реквізити тайника.
  Запис тікетів у Loopa — `loopa.надіслати_тікет` за контрактом із
  «Запитів до джерел», вмикається `LOOPA_WRITE=1`.
- **Не зроблено:** розділ «Тайники» на боці Loopa (їхня робота), розділ «Тайники» в Loopa,
  ABC через OLAP (замість нього — файл `abc*.csv` раз на місяць; Server
  API — одна сесія, і та в Core; не чіпати),
  графік змін і прив'язка до співробітника, перцептивні хеші фото
  (зараз — file_unique_id), реквізити виплат, автонабір із бази.
- Відкриті питання ТЗ (розділ 23): ставка за анкету і ліміти зараз у
  `налаштування.py` як 0 / 700 / 1000 — це заглушки до рішення.

## Сутності і файли стану (`~/.shopper`)

| файл | що |
|---|---|
| `тайники.json` | реєстр: Т-001, статус, згоди, інструктаж, рейтинг, кулдаун |
| `хвилі.json` | Х-001: слоти, відгуки, черга |
| `перевірки.json` | П-0001: завдання (копія цілком), анкета, приймання, оцінка, виплата |
| `налаштування.json` | те, що змінили з бота поверх `ТИПОВІ` |
| `журнал.jsonl` | рішення агента і людей — з «чому» |
| `доступ.json` | люди компанії та ролі |
| `сесії.json`, `режими.json` | де людина зупинилась у формі / що зараз вводить |
| `еталони.json` | база еталонів, якщо вивантажена; інакше зразок із репо |

Один номер на все: заявка на виплату — той самий номер з префіксом В-.

## Анкета (`анкета.json`)

Блоки з полем `етап` («до» — після оформлення, «після» — після
отримання) і `коли` (канал, або відповідь на попередній крок). Блок
`страви` динамічний: по кожній позиції завдання — ознаки з її еталону.
Після кожної відповіді кроки перебудовуються (`анкета.оновити`), тому
блок оператора з'являється, коли людина сказала «контакт був».

Бали (`оцінка.py`): група 0–100, «н/д» поза знаменником, `субєктивно`
— половина ваги, `критично` / `критичні` → незалік групи. Коди
`час_оформлення`, `час_отримання`, `обіцяний_час`, `сума_замовлення`,
`контакт_оператор`, `заг_враження`, `кух_коментар` — код знає їх по
імені (тайминг, виплата, суперечності). Перейменовувати — разом з ним.

## Викатка

`deploy/deploy.sh "що змінили"` — selftest локально, коміт, push, rsync,
selftest на сервері, збірка, рестарт. У сесії Claude в auto-режимі
об'єднана команда rsync+docker блокується — скрипт робить це окремими
кроками. Секрети (`data/api.env`) скрипт не чіпає.

## Правила

- **Перед деплоєм — `python3 selftest.py`.** Анкета й еталони, виклики
  неіснуючих функцій і атрибутів модулів, кнопки без обробника, і
  повний цикл на фальшивому Telegram (`tests/dryrun.py`).
- Персоналії тайника (ім'я, телефон, Telegram) у робочі чати не йдуть
  ніколи — лише Т-номер. `dryrun` це перевіряє.
- Агент сам: планує, видає завдання, повертає на доробку, формує
  заявку. Людина: активація, приймання, відхилення, блеклист, гроші.
  Кожна дія агента — у журнал з обґрунтуванням.
- Нижня клавіатура під час форми — «Назад / Пропустити / Скасувати».
  Скасування анкети зберігає чернетку.
- Робочі чати підключаються командою `/чат` у групі (потрібна роль
  з правом «правила»). Поки чат не підключений — його повідомлення
  йдуть маркетологам із позначкою.
- Бот говорить українською. Два запущені екземпляри ділять
  повідомлення навпіл — при перенесенні старий гасити.

## Loopa — це своє

Loopa (Review360) — власний продукт, код у `~/code/Review360` (FastAPI,
routers/, templates/admin.html), над ним працює сесія «Dev setup
configuration». Розділ «Тайники» там будується поверх `api.py` цього
бота за `docs/API.md`; тікети — їхній endpoint `/api/v1/mystery/tickets`.
Питання по інтеграції — тій сесії, не «розробникам Loopa».

## Доступи

Бот має ті самі ключі, що й Core, до всього, крім самого Core і
Джамшута: Syrve (сервер мережі, торгова точка, Cloud), Loopa, адмінка
Cappi, OpenAI. Це рішення власника (16.09.2026): «максимально можливий
доступ». Три наслідки, про які треба пам'ятати:

- **Ключ ТП створює накази про ціни.** Бот тайного гостя не має жодної
  причини щось писати в Syrve. Усі виклики — читання; будь-який запис
  туди — помилка проєктування, а не функція.
- **Сесій до Syrve Server API — не більше однієї одночасно.** Ліцензія
  дає три слоти на весь сервер, і Core тримає їх під себе. Замовлення
  шукати через Cloud API (`deliveries/by_delivery_date_and_phone`) —
  слотів він не займає.
- **Контейнер ізольований, ключі — ні.** Той, хто отримає цей
  `api.env`, отримає всю мережу. Файл лише на сервері, `chmod 600`,
  у git не потрапляє.

## Паралельні сесії

Над цим репозиторієм і сервером працює ще одна сесія Claude (вона
підняла контейнер і переносила ключі 16.09). Перед комітом і деплоєм —
`git fetch`, дивитись `git log origin/main`, не перезаписувати чужі
правки цілим файлом: 16.09 так ледь не зник розділ «Доступи».
