# Cappi Shopper — що треба знати, перш ніж щось міняти

Бот тайного гостя мережі Cappi (Одеса). Живий перевіряючий робить
замовлення, бот веде його по чек-листу і віддає адмінам звіт. Брат
Cappi-Core: та сама порода — чистий Python без залежностей, конфіг
у `api.env`, стан у файлах, самоперевірка перед деплоєм.

## Стан проєкту

Каркас від 16.09.2026. **ТЗ ще немає** — `сценарій.json` це заглушка з
правдоподібними питаннями. Коли ТЗ прийде, воно лягає у сценарій, код
не чіпаємо, якщо не з'явиться новий тип кроку.

Підключення (токен бота, група для звітів, сервер) — пізніше.

## Сценарій: формат

```
філії     список рядків — перший екран перевірки
канали    список рядків — другий екран; крок з полем «канали»
          показується лише на цих каналах
розділи   [{код, назва, кроки: [...]}]
крок      {код, тип, питання, обов=true, вага, критично, канали}
```

Типи кроків і що вони дають у бали:

| тип | відповідь | бал |
|---|---|---|
| `оцінка` | 1–5 кнопками або цифрою | (n−1)/4 від ваги |
| `так_ні` | кнопки; потрібне поле `очікується` | вага або нуль |
| `вибір` | `варіанти` + `бали` по кожному | частка від найбільшого |
| `число` | набирає | не оцінюється |
| `текст` | набирає | не оцінюється |
| `час` | кнопка «зараз» або `ГГ:ХХ` | не оцінюється |
| `фото` | надсилає фото | не оцінюється, іде у звіт |

Коди `час_замовлення`, `час_отримання`, `обіцяний_час`, `сума`,
`коментар` — звіт.py знає їх по імені (час доставки, запізнення, чек).
Перейменовувати — разом зі звітом.

`критично: true` — провал позначається у звіті окремо. Це не «мінус
бали», це привід дзвонити гостю.

## Правила

- **Перед деплоєм — `python3 selftest.py`.** Ловить зламаний сценарій,
  кнопку без обробника, виклик неіснуючої функції і ганяє повну
  перевірку на фальшивому Telegram. У Cappi-Core правки текстовою
  заміною двічі з'їдали сусідні функції — бот виглядав живим, кнопки
  мовчали.
- Нижня клавіатура «Назад / Пропустити / Скасувати» вішається один раз
  на початку перевірки і знімається після звіту. Inline-кнопки під
  питанням її не прибирають — не треба слати порожні повідомлення.
- Незавершена перевірка переживає перезапуск (`сесії.json`): гість
  може чекати кур'єра півгодини.
- Чужий, що написав боту, не дізнається нічого, а адмін бачить картку з
  кнопкою «Додати». Id людини шукати не треба.
- Бот говорить українською. Коментарі в коді — як зручно.
- Два запущені екземпляри ділять повідомлення навпіл — при переносі
  старий гасити.

## Звіт для Core

Запис у `перевірки.jsonl` — це те, що потім читатиме Cappi-Core для
показника «тайний гість» (там він вноситься руками). Поля:
`id, почато, завершено, хто, філія, канал, відповіді, оцінка{відсоток,
розділи, критичні, час_доставки_хв}`. Ламати формат — узгодити з Core.
